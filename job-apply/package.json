{
  "name": "job-auto-apply",
  "version": "1.0.0",
  "description": "Auto-apply to jobs on Indeed and LinkedIn using Playwright",
  "main": "index.js",
  "scripts": {
    "install-browser": "npx playwright install chromium",
    "apply": "node index.js",
    "apply:dry": "node index.js --dry-run"
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "dotenv": "^16.4.5"
  }
}
